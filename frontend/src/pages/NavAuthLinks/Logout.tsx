import { Button } from "../../components/Button"

export const Logout = () => {
  const handleLogout = () => {
    
  }
  return (
    <>
      <Button onclick={handleLogout} text="LogOut"></Button>
    </>
  )
}
